<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.business.system.member.mapper.MemberMapper">
	
	<sql id="Example_Where_Clause">
		<where>
			<foreach collection="oredCriteria" item="criteria" separator="or">
				<if test="criteria.valid">
					<trim prefix="(" prefixOverrides="and" suffix=")">
						<foreach collection="criteria.criteria" item="criterion">
							<choose>
								<when test="criterion.noValue">
									and ${criterion.condition}
								</when>
								<when test="criterion.singleValue">
									and ${criterion.condition} #{criterion.value}
								</when>
								<when test="criterion.betweenValue">
									and ${criterion.condition} #{criterion.value} and
									#{criterion.secondValue}
								</when>
								<when test="criterion.listValue">
									and ${criterion.condition}
									<foreach close=")" collection="criterion.value" item="listItem"
										open="(" separator=",">
										#{listItem}
									</foreach>
								</when>
							</choose>
						</foreach>
					</trim>
				</if>
			</foreach>
		</where>
	</sql>

  <select id="selectMemberListByExample" parameterType="tk.mybatis.mapper.entity.Example" resultType="org.business.system.member.model.dto.MemberDto">
select  a.id as id,a.member_name as memberName,a.parent_id as parentId,a.create_date as createDate,a.remark as remark,
        a.member_type as memberType, a.member_state as memberState,a.invite_code as inviteCode,a.member_no as memberNo,
        b.pay_account as payAccount  
       from t_system_member a
       left join t_system_user b on a.user_id = b.id 
      <include refid="Example_Where_Clause" >
      </include>
  </select>
</mapper>